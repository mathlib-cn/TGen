#include <stdio.h>
#include <stdlib.h>
#include <math.h>
//#include "myhead.h"
/*
void binaryshow(double);
void getbinary(_UL, int *);
void printbinary(int *);
double computeULP(double);

int main(){
	double ulp, x;
	double x1, x2;
  _UL temp1, temp2;
	int i, t, binary[64];
	
	x = 2;
	ulp = computeULP(x);
	printf("binary of %f's ulp is ", x);
	binaryshow(ulp);

	x1 = 1.5;
	x2 = -1.4;

	temp1 = ftoid(x1);
	temp2 = ftoid(x2);
	getbinary(temp1, binary);
	printbinary(binary);
	getbinary(temp2, binary);
	printbinary(binary);
	
	t = computeULPDiff(x1, x2); 
//	t = temp1 - temp2;
//	t = abs(t);
	printf("t = 0x%X\n", t);
	
	return 0;
}
*/
